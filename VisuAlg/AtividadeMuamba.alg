algoritmo "Creuza04"
      //Creuza comprou muita muamba
var
      valor1, imposto: real

inicio
      Escreva("Qual o valor do produto: US$ ")
      Leia(valor1)
      imposto <- ((valor1 * 60) / 100)
      Escreva(" Você terá que pagar US$", imposto:4:2, " dólares de imposto!")

fimalgoritmo
